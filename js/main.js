async function succession() {
  // request_busstop_time();
  const buspole_data = await buspole_main();
  console.log(buspole_data);
  // const buspoledata = await call_bus_data();
  // console.log(buspoledata);

  // const cat = await request_dog();
  // console.log(cat);
  // downloadJson();

  // envdata = require("../.env");
  // console.log(envdata);
  // const now = timeread();
  // console.log(now);
  // console.log(comment);
}
